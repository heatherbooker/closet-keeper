//clothing constructor

function Clothing(keywords, type, use, color, imageFilePath) {
    this.keywords = keywords;
    this.type = type;
    this.use = use;
    this.color = color;
    this.image = imageFilePath;
}